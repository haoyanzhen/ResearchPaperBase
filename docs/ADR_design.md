# design for paper push agent

Author: haoyanzhen
Date:   2026-02-18
Update: 2026-04-05

负责核心设计，详细设计若与该文档设计冲突，以该文档为准。

## 设计目标

实现课题组级的论文BASE库构建，提供论文的检索、下载、解析、互动问答、自动推送等功能，成为科研人员安全、可靠、有用的学术论文信息基石。

## 数据总体设计

所有数据的总体单元为用户；
用户可以建立多个课题，不同用户的课题互不相关，每个课题内部独立，所有模式以课题为基础，即以课题为单元，构建、研究等均在课题内部进行；
论文库由各个课题下载的论文共同组成，另外建立关联库记录课题和论文的关联

## 框架设计

总体框架设计为基础层+多模式的形式，基础层一直启用，随时有效；多模式中必须启用且仅启用一个。

多模式目前设计有三种：构建模式、综述模式、深度研究模式。
其中构建模式为用户构建数据库的工作流模式，核心是通过agent工作流构建论文数据库作为base；
综述模式为基于当前课题关联的论文集，进行综述框架设计与综述文章撰写；
深度研究模式为向用户提供论文数据库base的交互式问答，核心是问答互动，可包含多种倾向设定（理论分析、技术讨论、实验设计等）。

## 构建模式基础逻辑设计

|阶段|执行方式|逻辑说明|
|---|---|---|
|检索词生成|内置调用|将用户课题输入给 LLM，一次性生成搜索关键词，并针对 arXiv、OpenAlex、Semantic Schola、astrophysics data system 生成多种高覆盖率的 Query 列表|
|检索与汇总|工具执行|顺序调用各数据库 API，将检索结果进行解析，得到每一篇论文的标题、作者、出版时间、期刊、摘要等内容，汇总到统一的 List 中|
|评分与筛选|顺序执行|将 List 中的所有文献的标题和摘要进行阅读，对其对研究方向的参考价值进行评分，评分>7则认为有效并筛选出来，对比数据库并进行去重，得到新增论文列表|
|下载与解析|顺序执行|按照新增论文列表，逐一尝试下载 PDF 并解析，作为论文的文本信息。若失败则记录原因，并以摘要为该论文的文本信息。|
|总结生成|内置调用|调用LLM对论文的文本信息进行一句话总结、亮点解析、相关性要点、技术方法与创新等信息提取|
|格式化与储存|工具执行|对数据库进行增量更新|
|邮件发送|工具执行|提取数据库中未发送过的论文数据，将其转化为 HTML 邮件发送到指定接收邮箱|

## RAG(Retrieval Augmented Generation)

论文数据库的建立可分两种类型，一个是用于公用更新的关系型数据库，一个是用于作为LLM大模型读取数据库的向量数据库

|组件|存储内容|用途|
|---|---|---|
| 关系型数据库 |DOI、标题、推送状态、抓取时间|负责 流程管理（去重、定时推送、状态追踪）|
| 向量数据库 |论文全文/摘要的向量数据|负责 知识问答（语义搜索、纵向对比分析）|

在建立向量数据库之后，可通过词语提取
