# record of update this project with VibeCoding tools

Author: haoyanzhen
Date:   2026-04-04

## spec

**2026-04-03**

#design.md 根据这份design设计，给出完整的设计： 1. 各类框架（如前后端、agent框架）的选择原因和竞品对比。 2. 前端界面需要包含模型选择和API配置功能 3. 前端界面需要能够直接查看已经检索过的所有课题的条目和查阅对应的db 4. 前端界面需要在每一个独立的大步骤时暂停并让用户参与互动和修改

按照当前设计，完成需求规格说明（并存为文件）

（check spec）

在spec.md中，新增两个内容： 1. 新增完整的底层设计。补充：由于该项目以科学研究主题为核心单元，因此底层设计中也以科学研究主题为核心单元，状态管理、生命周期管理和接口协议都需要遵守科学研究主题信息，相应的所有检索信息、数据库信息等也均以此为基础设计。 2. 新增详尽的错误处理设计和状态监控设计。 继续补充依然需要补充的内容，完成后进入实现方案创建

（check 数据库设计）

**2026-04-04**

重构6.1数据字典： - 用户表，唯一 - 与用户关联的配置表，唯一，保存通用的基础配置，以及每位用户自己自己更新的配置。每一行为配置id+用户id+配置名+配置值+更新时间 - 与用户关联的课题任务表，唯一，保存每位用户各个课题及其进展 - 与课题关联的检索词表，唯一，保存每个课题的多种仓库的检索词，每一行为一个检索词 - 论文表，唯一，保存所有论文的元数据的数据库 - 课题-论文关联表，唯一，保存所有课题id-论文id的关联信息

（check 数据库设计）

检查6.1的表格是否对齐，如有未对齐的进行补齐

（check 数据库设计）

底层还应该再次修改 1. 设计 - 以研究主题为基础单元的设计依然保留 - 新增设计：整个研究主题的构建分为两套模式，其一为构建模式，其二为深度研究模式 - 在构建模式中，用户可以借助Agent工具设计检索词，补充论文或手动上传资料，并通过Agent自动完成解析和后台入库，通过邮件提示服务接收新的信息 - 在深度研究模式中，用户可以深入与LLM探讨理论与技术，开拓新的方向，设计周密的实验（不采用简单的向量检索，而采用 Graph-RAG） - 两套模式应当有明显的切换方式，以方便使用 2. 新增数据表，挂载在 project_id 下： 1. research_history 表： - 存储与用户的对话记录 2. research_dialogues 表：存储用户与 LLM 的深度探讨记录。 - 对探讨的总结，并引用 papers 或网页搜索结果或用户提供的信息&判断等。 3. session管理中应该将API对齐到不同模式，采用API路由隔离，对应的context应随着模式变化而自动更新 4. 在深度研究模式中，调研RAG引擎与深度研究模式的适配性 - 调研 Graph-RAG 与研究的适配性并给出是否采用意见 - 调研 Relational-RAG 与研究的适配性并给出是否采用意见 5. 新增可扩展性：用户的讨论或用户撰写的观点意见可发表在推荐模块供其他用户参考（但禁止讨论，只允许暴露用户名或邮箱以用于私下交流） 根据以上更新，重新修改spec设计

（check 更新总结）
（check 引言与总体设计）

根据上述底层修改，更新项目的设计目标和需求，进一步更新所有章节

（check 功能需求）

#sepc.md 以当前的双模式为基础，增加补充设计说明： 1. 用户和用户信息管理、配置管理独立于双模式之外，全局通用，作为基础层； 2. 检索历史、与自己课题关联的数据库查看也独立于双模式之外，随时可以打开或关闭； 3. 对已有的功能进行分类，确定其所属的模式或层级 4. 双模式只能选择一个，且必定有一个。这里应当预留扩展性，为更新为三模式或更多提供修改空间。

（check 总体设计）
（check 功能需求）

#sepc.md 以当前设计为基础，增加补充一些功能： 1. 调研并思考深度研究模式内对话式探讨是否应该具备不同模式，如理论分析模式、技术讨论模式、实验方案设计模式。 2. 深度研究模式也应当有agent总结模块以及数据导出功能。 3. 可扩展性需求中需要加入用户间交互 4. 对3功能需求中已有的功能需求进行分类，确定其所属的模式或层级

（check 功能需求）

删除FR-027 用户间交互功能，对3功能需求中已有的功能需求进行分类，确定其所属的模式或层级，并以此更新3功能需求

（check 功能需求）

在#spec.md中仅保留核心定义和限制性说明，将其他各类细节、代码示例等转移到其他文档并保存在docs文件下

（check docs/错误处理设计文档， docs/底层设计详细文档，docs/状态监控设计文档，docs/spec 8到10章）

除了构建模式和深度研究模式外，新增一个模式：主题综述模式，其工作模式类似构建模式，为Agent主导的工作流构建，其工作内容为根据用户的课题，设计综述文章架构、用户审查、撰写每章内容并引用、Agent自动审查迭代、将每章内容汇总成完整的综述文章。将设计更新在spec中

（check 功能需求）

将3.功能需求中的每个需求名称改为模块-需求名称的格式，如基础层-数据导出，并按照层级进行重新排序，层级排序顺序为基础层、构建模式、综述模式、深度研究模式，并重新编号

（check 功能需求）
