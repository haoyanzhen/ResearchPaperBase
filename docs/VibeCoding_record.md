# record of update this project with VibeCoding tools

Author: haoyanzhen
Date:   2026-04-04

## spec

**2026-04-03**

#design.md 根据这份design设计，给出完整的设计： 1. 各类框架（如前后端、agent框架）的选择原因和竞品对比。 2. 前端界面需要包含模型选择和API配置功能 3. 前端界面需要能够直接查看已经检索过的所有课题的条目和查阅对应的db 4. 前端界面需要在每一个独立的大步骤时暂停并让用户参与互动和修改

按照当前设计，完成需求规格说明（并存为文件）

（check spec）

在spec.md中，新增两个内容： 1. 新增完整的底层设计。补充：由于该项目以科学研究主题为核心单元，因此底层设计中也以科学研究主题为核心单元，状态管理、生命周期管理和接口协议都需要遵守科学研究主题信息，相应的所有检索信息、数据库信息等也均以此为基础设计。 2. 新增详尽的错误处理设计和状态监控设计。 继续补充依然需要补充的内容，完成后进入实现方案创建

（check 数据库设计）

**2026-04-04**

重构6.1数据字典： - 用户表，唯一 - 与用户关联的配置表，唯一，保存通用的基础配置，以及每位用户自己自己更新的配置。每一行为配置id+用户id+配置名+配置值+更新时间 - 与用户关联的课题任务表，唯一，保存每位用户各个课题及其进展 - 与课题关联的检索词表，唯一，保存每个课题的多种仓库的检索词，每一行为一个检索词 - 论文表，唯一，保存所有论文的元数据的数据库 - 课题-论文关联表，唯一，保存所有课题id-论文id的关联信息

（check 数据库设计）

检查6.1的表格是否对齐，如有未对齐的进行补齐

（check 数据库设计）

底层还应该再次修改 1. 设计 - 以研究主题为基础单元的设计依然保留 - 新增设计：整个研究主题的构建分为两套模式，其一为构建模式，其二为深度研究模式 - 在构建模式中，用户可以借助Agent工具设计检索词，补充论文或手动上传资料，并通过Agent自动完成解析和后台入库，通过邮件提示服务接收新的信息 - 在深度研究模式中，用户可以深入与LLM探讨理论与技术，开拓新的方向，设计周密的实验（不采用简单的向量检索，而采用 Graph-RAG） - 两套模式应当有明显的切换方式，以方便使用 2. 新增数据表，挂载在 project_id 下： 1. research_history 表： - 存储与用户的对话记录 2. research_dialogues 表：存储用户与 LLM 的深度探讨记录。 - 对探讨的总结，并引用 papers 或网页搜索结果或用户提供的信息&判断等。 3. session管理中应该将API对齐到不同模式，采用API路由隔离，对应的context应随着模式变化而自动更新 4. 在深度研究模式中，调研RAG引擎与深度研究模式的适配性 - 调研 Graph-RAG 与研究的适配性并给出是否采用意见 - 调研 Relational-RAG 与研究的适配性并给出是否采用意见 5. 新增可扩展性：用户的讨论或用户撰写的观点意见可发表在推荐模块供其他用户参考（但禁止讨论，只允许暴露用户名或邮箱以用于私下交流） 根据以上更新，重新修改spec设计

（check 更新总结）
（check 引言与总体设计）

**2026-04-05**

根据上述底层修改，更新项目的设计目标和需求，进一步更新所有章节

（check 功能需求）

#sepc.md 以当前的双模式为基础，增加补充设计说明： 1. 用户和用户信息管理、配置管理独立于双模式之外，全局通用，作为基础层； 2. 检索历史、与自己课题关联的数据库查看也独立于双模式之外，随时可以打开或关闭； 3. 对已有的功能进行分类，确定其所属的模式或层级 4. 双模式只能选择一个，且必定有一个。这里应当预留扩展性，为更新为三模式或更多提供修改空间。

（check 总体设计）
（check 功能需求）

#sepc.md 以当前设计为基础，增加补充一些功能： 1. 调研并思考深度研究模式内对话式探讨是否应该具备不同模式，如理论分析模式、技术讨论模式、实验方案设计模式。 2. 深度研究模式也应当有agent总结模块以及数据导出功能。 3. 可扩展性需求中需要加入用户间交互 4. 对3功能需求中已有的功能需求进行分类，确定其所属的模式或层级

（check 功能需求）

删除FR-027 用户间交互功能，对3功能需求中已有的功能需求进行分类，确定其所属的模式或层级，并以此更新3功能需求

（check 功能需求）

在#spec.md中仅保留核心定义和限制性说明，将其他各类细节、代码示例等转移到其他文档并保存在docs文件下

（check docs/错误处理设计文档， docs/底层设计详细文档，docs/状态监控设计文档，docs/spec 8到10章）

除了构建模式和深度研究模式外，新增一个模式：主题综述模式，其工作模式类似构建模式，为Agent主导的工作流构建，其工作内容为根据用户的课题，设计综述文章架构、用户审查、撰写每章内容并引用、Agent自动审查迭代、将每章内容汇总成完整的综述文章。将设计更新在spec中

（check 功能需求）

将3.功能需求中的每个需求名称改为模块-需求名称的格式，如基础层-数据导出，并按照层级进行重新排序，层级排序顺序为基础层、构建模式、综述模式、深度研究模式，并重新编号

（check 功能需求）

**2026-04-06**

（check 非功能需求）
（check 系统约束）

只读取2.项目概述和4.1性能需求，对比不同的前后端、Agent框架、LLM集成、数据库框架，给出推荐技术栈并给出推荐理由。将推荐技术栈选择更新在5.1中

（check 系统约束）

**2026-04-07**

（check 系统约束之后，删除没必要的模块，更新数据表需求）

设计论文的关系型数据库、论文数据库和图数据库格式，并更新到#docs/spec.md #6. 数据需求中

**2026-04-08**

（check 数据需求）

根据 ADR_design， 更新并校对spec，并在完成后提出改进建议

（check 数据需求）

在6.数据需求中，执行 stage_records 表扩展，同时增加深度研究讨论历史数据表、综述模式工作记录表的设计

（check 数据需求）

对话表应当完全记录原样的对话历史信息，每一行对应一轮对话（即用户-Agent各说一次）。据此对深度研究对话表进行修改

（check 数据需求）

进行主要函数的api设计和前后端通信设计，写入docs/api.md中

（check docs/api.md 总结）

再次回顾spec，找出设计上可以改进的点，寻找是否有重要漏洞

（check AI意见）

根据你给出的建议，逐条做出回应建议，根据这些建议更新设计，并写入 spec.md 中：
1. 考虑到保存正在运行中的任务在开发上或许有难度，因此模式切换时，若有正在进行的任务，并不暂停任务或中止任务，而是先给出确认弹窗，提示用户有正在进行的任务，并给出两个选项：留在页面等待执行完成 & 离开并退出任务。根据用户的选择来决定是否中止任务并切换模式。
2. 删除FR-28
3. 增加切换必要提示设计：当论文库构建未完成时，切换到研究模式或综述模式会给出不可用提示，并给出按钮来返回构建模式的下一个需要执行的步骤。
4. 分析以下两个路径哪个更好？：去掉`projects.current_stage`和`projects.mode`；将projects这两个字段与stage_records进行关联
5. 暂时不对embedding模型进行锁死设计，之后再议
6. 将4.5数据库扩展这一句删掉
7. 修正章节编号
8. 删除`projects.push_status`字段
9. 调度的单元是项目，因此需要重新设计该表的字段，方便项目进行自动调度
10. 删除第7节，因为已有独立文档记录API设计
11. 将`max_context_length`改到70页内容左右；深度研究模式中长对话将另外设计，再议
12. 同一用户应当能够同时运行多个project的构建任务，服务器的核心数足够，只是带宽有限，并发时论文下载较慢
13. 测试基准设计应当小一些，方便测试；最终的验收测试中需要按照最大设计来进行测试保证可用性
14. 向量数据库若和关系型数据库同步失败，则立刻启动回滚，不允许任何部分成功状态，需要在任何时候保持一致。图数据库不需要和前两者保持一致，可有些许滞后或仅仅是局部的增量更新，每日深夜在后台自动重构完整图数据库（如有需要）。

（check spec）

根据spec的需求，设计前端界面草稿，存为新的文件

**2026-04-20**

（check ui_design）

知识图谱构建应当在构建模式中进行，根据此条在docs/ADR_desgin中进行修改。

（check ADR）

检查ADR_design是否完善，评价其设计。

（check ADR）

补充信息：
框架设计：三种模式互斥是因为三种模式运行的逻辑截然不同，每种模式有自己的agent链，因此需要互斥。

根据上述评价和补充信息完善ADR_design

（check ADR）

根据ADR_design修改docs/spec.md

（check spec）

检查并评价spec设计

（check spec）

根据以上评价补充信息：
1. FR-007的确是移除关联
2. FR-009缺失，需要补充：可选择导出论文库内容为excel文件或者导出论文集
3. 重新设计project.status字段，设计各类状态触发的条件
4. FR-018的本意为完成构建模式后立刻向使用者的邮箱发送一封包含当前所有已查询论文信息的邮件，而定时任务针对的是每日的自动查询和更新
5. FR-010中定时任务不仅仅是定时发送，应当包含按照构建模式保存的有效的提示词进行自动搜索、评分筛选、下载解析、AI分析、数据存储、邮件提示等无人工参与的全任务流程。
6. 取消支持跳过当前步骤
7. 构建模式"用户参与度"改为中
8. 将FR-026合并进 FR-027 作为实现细节
9. 重新整理FR说明顺序

按照以上信息修改spec

（check spec）

给出建议：在有限的AI上下文下，根据spec来使用AI驱动编写一个完整的包含前后端的web app时，应当如何拆分任务保证该系统设计中各个模块的互通性不出错呢？

```plaintext
回复：第一阶段：先生成契约文件（不写业务代码）：

1. docs/schema.sql — 完整的数据库建表语句（含约束、索引、注释）
每次 AI 开发数据库相关功能时，只需喂这一个文件，不需要读完整 spec。

2. docs/api.md（spec 已指向此文件）— 每个端点的 Request/Response 结构、状态码、错误格式
前端、后端、测试三方的唯一参考。格式建议用 OpenAPI YAML，可直接生成类型。

3. frontend/types/index.ts（或 types.py） — 所有跨模块共享的数据结构定义
例如 Paper、Project、DialogueTurn、BuildStage 等。这是前后端的类型锚点。

第二阶段：按依赖层级拆分任务

数据层 → 服务层 → API层 → 前端状态层 → UI组件层
每一层只依赖下层契约，不依赖下层实现。拆分原则：

任务粒度	上下文需要喂什么
数据库迁移脚本	schema.sql
单个 service（如 PaperService）	schema.sql + 该 service 的 FR 段落
单个 API 路由	api.md 对应端点 + service 接口签名
前端单个 page/feature	api.md 对应端点 + types/index.ts
Agent 链（构建/综述/深研）	对应模式的 FR 段落 + service 接口签名

第三阶段：按模式边界切割，每个模式独立交付
本项目三种模式 Agent 链互不相干，是天然的并行开发单元：

基础层（FR-001~011）         ← 最先开发，其他模式依赖它
    ↓
构建模式（FR-012~019）       ← 优先级最高，写入三个数据库
    ↓
深度研究模式（FR-025~028）   ← 依赖构建模式产出的数据
综述模式（FR-020~024）       ← 依赖构建模式产出的数据（可并行）

具体操作建议
每次给 AI 的上下文模板：

任务：实现 [具体功能，如 FR-015 论文评分与筛选]
契约文件：docs/file_map.md + [粘贴 api.md 相关端点]
已有接口：[粘贴依赖的 service 函数签名，不粘贴实现]
要求：根据修改的文件，更新docs/file_map.md
```

根据spec的第6章编写完整的数据库建表语句，并生成契约文件docs/schema.sql

（check schema）

检查schema.sql是否与spec中的设定一致，特别是在继承性和独立性方面

（check output）

1. keywords.is_searched DEFAULT在两个文件里都改为FALSE
2. recommendations.project_id应当删除，推荐模块是基础层，只和用户相关，和课题无关
3. apers → project_paper_relations 用 ON DELETE RESTRICT
4. project_paper_relations.is_valid DEFAULT 在两个文件里改为 FALSE

（check spec & schema）

根据spec生成所有跨模块共享的数据结构定义，并写入frontend/types/index.ts

（check）

检查spec和index.ts的一致性

（check）

```
4. 在TypeScript层面用判别联合
5. 在spec中删除记录模式切换历史这一条
6. 对FR-028设计指定的JSON结构化类型
```

（check spec）

## code

**2026-04-21**

任务：实现基础层基本架构，完成基础层架构搭建
信息：
**3.2.1 全局基础层功能**（独立于三模式，全局通用）
- FR-001：基础层-用户注册与登录
- FR-002：基础层-模型配置管理
- FR-003：基础层-论文数据库API配置
- FR-004：基础层-邮件配置管理
- FR-005：基础层-三模式切换
- FR-006：基础层-检索历史查看
- FR-007：基础层-数据库查看与管理
- FR-008：基础层-任务管理
- FR-009：基础层-数据导出
- FR-010：基础层-定时任务
- FR-011：基础层-推荐模块
契约文件：docs/schema.sql, docs/api.md

（check）

**3.2.1 全局基础层功能**（独立于三模式，全局通用）
- FR-001：基础层-用户注册与登录
- FR-002：基础层-模型配置管理
- FR-003：基础层-论文数据库API配置
- FR-004：基础层-邮件配置管理
- FR-005：基础层-三模式切换
- FR-006：基础层-检索历史查看
- FR-007：基础层-数据库查看与管理
- FR-008：基础层-任务管理
- FR-009：基础层-数据导出
- FR-010：基础层-定时任务
- FR-011：基础层-推荐模块

在基础层架构上实现以上功能

（check）

检查FR-001到FR-011的前后端实现是否完整

（check）

根据schema.sql，搭建数据层

（check）

检查数据层和schema.sql的设计是否保持一致，补充简明扼要的项目级注释、函数级注释、块级注释

（check）

制作项目文件功能目录，存在docs/新文件下作为参考契约文件。

（check docs/file_map）

任务：实现构建模式框架搭建
契约文件：docs/file_map.md + docs/spec.md#3. 功能需求 + docs/api.md#4. 构建模式 + docs/ui_design.md#P04 · 构建模式主界面
要求：根据修改的文件，更新docs/file_map.md

（check）

任务：检查构建模式框架实现与spec设计是否一致，完成构建模式FR-012到FR-019的功能实现
契约文件：docs/file_map.md + docs/spec.md#3. 功能需求 + docs/api.md#4. 构建模式 + docs/ui_design.md#P04 · 构建模式主界面
要求：根据修改的文件，更新docs/file_map.md

**2026-04-22**

（check）

任务：搭建Agent层，并将其按照模式进行结构上的文件/文件夹命名区分。先完成构建模式的Agent功能实现。
契约文件：docs/file_map.md + docs/spec.md#3. 功能需求 + docs/api.md#4. 构建模式 + docs/ui_design.md#P04 · 构建模式主界面
要求：根据修改的文件，更新docs/file_map.md；Agent提示词以及LLM相关参数应当单独存为配置文件

（check）

任务：检查Agent层中关于构建模式的实现是否与契约文件一致
契约文件：docs/file_map.md + docs/spec.md#3. 功能需求 + docs/api.md#4. 构建模式 + docs/ui_design.md#P04 · 构建模式主界面
要求：根据修改的文件，更新docs/file_map.md；Agent提示词以及LLM相关参数应当单独存为配置文件

（check file_map, spec）

任务：在docs/spec.md中进行设计修改：在基础层中新增管理员配置，用户第一次登录时可设置管理员账号，管理员账号应当具备额外的管理员配置面板，管理员配置应当包含默认模型设置、论文库API设置、管理其他用户的账号等功能。
要求：修改docs/spec.md的## 3. 功能需求和## 6. 数据需求，在数据需求中新增管理员配置数据表。

（check spec）

任务：在数据层、基础层前后端和API等环节中对管理员功能进行功能实现与文件更新，需要完全完成管理员功能
设计: （copy了docs/spec.md中的管理员功能相关设计）
要求：根据修改的文件，更新docs/file_map.md

（check file_map）

检查管理员功能实现与设计是否一致

（check）

**2026-04-23**

任务：检查研究主题模块完成度，并补充相应的前端、数据库、Agent、API等实现。
契约文件：docs/file_map.md + docs/spec.md#3. 功能需求 + docs/spec.md#6.1 关系型数据库+ docs/api.md#3. 研究主题（Project）模块+ docs/schema.sql
要求：根据修改的文件，更新docs/file_map.md

（check file_map）

任务：检查认证模块和配置模块完成度，进行补充、修复或删改
契约文件：docs/file_map.md + docs/api.md#1. 认证模块 + docs/api.md#2. 配置模块 + docs/schema.sql
要求：根据修改的文件，更新docs/file_map.md

（check file_map）

任务：实现主题综述模式框架搭建 + 功能 FR-020 到 FR-024 实现
契约文件：docs/file_map.md + docs/spec.md#3. 功能需求 + docs/api.md#7. 综述模式 + docs/ui_design.md#P09 · 综述模式主界面
要求：根据修改的文件，更新docs/file_map.md

（check）

任务：检查主题综述模式实现是否正确
契约文件：docs/file_map.md + docs/spec.md#3. 功能需求 + docs/api.md#7. 综述模式 + docs/ui_design.md#P09 · 综述模式主界面
要求：根据修改的文件，更新docs/file_map.md；检查api、前端、后端、数据库等实现，是否留有隐患

（check）

任务：实现深度研究模式框架搭建 + 功能 FR-025 到 FR-028 实现
契约文件：docs/file_map.md + docs/spec.md#3. 功能需求 + docs/api.md#6. 深度研究模式 + docs/ui_design.md#P07 · 深度研究模式主界面
要求：根据修改的文件，更新docs/file_map.md

**2026-04-23**

（check）

任务：检查深度研究模式实现是否正确
契约文件：docs/file_map.md + docs/spec.md#3. 功能需求 + docs/api.md#6. 深度研究模式 + docs/ui_design.md#P07 · 深度研究模式主界面
要求：根据修改的文件，更新docs/file_map.md；检查api、前端、后端、数据库等实现，是否留有隐患

（check）

根据 docs/spec 生成一份包含状态检查、测试、错误报告等多种过程与结果审查机制设计，并选取能够实时、细致输出错误信息和检查错误原因的设计版本，并保存在docs的新文档中。

（check qa_design）

根据qa_design，完成 3. 错误码体系 到 6. 诊断快照端点（Inspector） 的框架搭建、功能实现和API对齐

（check）

任务：根据qa_design，完成 7. 分层测试策略 到 9. 日志规范 的框架搭建、功能实现和API对齐
要求：根据修改的文件，更新docs/file_map.md

（check）

任务：根据qa_design#10. 验收检查清单检查完成情况
要求：根据修改的文件，更新docs/file_map.md

（check）

任务：根据file_map#未实现（存根 / 待后续阶段开发），逐个检查其条目，并进行开发、修正或补充
要求：根据修改的文件，更新docs/file_map.md

（check）

为什么当前项目下并行存在src和backend两个文件夹？

```Plaintext
重构已完成：src/ → frontend/
agent_paperpush/
├── backend/        ← Python FastAPI
├── frontend/       ← TypeScript API 客户端 + 类型 + 组件
│   ├── api/
│   ├── types/
│   └── components/
├── e2e/
└── docs/
```

按照这个对称命名方案进行项目重构，并检查API。在修改之后，根据修改更新docs/file_map.md

（check）

任务：检查任务管理模块、系统的调度层等是否功能实现完毕，检查是否存在bug并修正。
契约文件：docs/file_map.md + docs/api.md
要求：根据修改的文件，更新docs/file_map.md

（check）

任务：根据 docs/schema.sql 逐条检查数据层是否实现完毕，检查是否存在bug并修正。检查数据调度层是否功能齐全，是否存在bug并修正。
契约文件：docs/file_map.md + docs/schema.sql
要求：根据修改的文件，更新docs/file_map.md

（check）

任务：根据 docs/ui_design.md 逐条检查其中包含的设计是否实现完毕，检查是否存在bug并修正。若有不在该文档里的新前端，检测其实现并更新 ui_design.md
契约文件：docs/file_map.md + docs/ui_design.md
要求：根据修改的文件，更新docs/file_map.md

（check）

**2026-04-28**(转到codex)

任务：参考 docs/ui_design.md 实现前端，检查普通用户和管理员的服务层是否实现完毕，该 web app 是否可以正常打开并访问。
参考文件：docs/file_map.md, docs/api.md, docs/ui_design.md, frontend/api/*
要求：根据修改的文件，更新docs/file_map.md，docs/ui_design.md

1. 审计上述实现。
2. 设计前端的各项单元测试，测试每个面板和交互能否正常响应。

根据当前的项目情况，整理并书写一版包含用户如何使用该app的安装、配置和启动的README.md

审计当前设置界面中邮件配置的权限，发送邮箱的配置理应只有管理员能够设置。

常规的 web app design 是如何分层构建完整设计的？审计设计文档，评估是否能够在没有额外信息的前提下实现完整的 web app 并且不引起恶心bug或设计冲突？如果不能，还缺失哪些设计？将审查报告更新到 docs/design_audit.md 中。（设计文档包括 docs/ADR_design.md, docs/spec.md, docs/api.md, docs/schema.sql, docs/ui_design.md, docs/qa_design.md）（若存在缺失，更新到 design_audit 中）

1. 按照这样的分层，生成一个设计分层参考模版文件
2. 按照 参考模版 对整个 docs 中的设计文档进行内容重整和结构重调
3. 按照 design_audit 对修改后的 docs 设计文档中缺失的设计进行重新审计，并生成选择和建议文稿，保存为新的文件等待人工确认

根据 design_layers 对整个 docs 的设计文档进行内容重整和重命名，生成新的按照分层来指定的设计文档群。

## re-design

**2026-04-29**

常规的 web app design 是如何分层构建完整设计的？审计设计文档，评估是否能够在没有额外信息的前提下实现完整的 web app 并且不引起恶心bug或设计冲突？如果不能，还缺失哪些设计？将审查报告更新到 docs/design_audit.md 中。（设计文档包括 docs/ADR_design.md, docs/spec.md, docs/api.md, docs/schema.sql, docs/ui_design.md, docs/qa_design.md）（若存在缺失，更新到 design_audit 中）

对于简单的包含用户管理和数据流、状态控制的web app，是否也需要这样的分层design？

1. 按照这样的分层，生成一个设计分层参考模版文件
2. 按照 参考模版 对整个 docs 中的设计文档进行内容重整和结构重调
3. 按照 design_audit 对修改后的 docs 设计文档中缺失的设计进行重新审计，并生成选择和建议文稿，保存为新的文件等待人工确认

那么在这样的分级中，是否需要需求文档，还是说需求文档本身不算设计文档，而是算作设计的参考？

> 需求文档算设计工作的上游输入，也可以放进设计文档体系里管理，但不要把它当成详细设计本身。

好的，根据 design_layers 对整个 docs 的设计文档进行内容重整和重命名，生成新的按照分层来指定的设计文档群。

缺失层用占位文档代替。

提问：像产品功能需求和类似 FR 的子例，在00_index 中展示的哪一层呢？

> 产品功能需求和 FR 子例主要属于 产品与领域层

正常的FR功能应当在哪一层呢？还是说FR设计要和分层相互独立？

> 正常的 FR 应该独立成需求层的一部分，但要通过追踪矩阵映射到各设计层。

这个项目的FR设计是否完全且必要？

> 不完全，也不全都必要。
> 当前 FR 覆盖面很广，已经能表达产品野心，但还不是一套干净的“必要且完整”的功能需求集。它更像是“产品功能 + 技术方案 + 实现细节 + 后续扩展”的混合清单。
> 当前 FR 设计“覆盖较全”，但“必要性未分级、完整性未闭环、粒度不一致”。

根据 02 文件的第2章 项目概述 设计，重新设计 第三章 功能需求

将FR-012拆分成两个：检索词生成、多源检索。将FR-019取消，内容拆分到各个构建模式的FR功能中。将FR-026取消，同样将内容拆分到各个主题综述模式中的FR中。将FR-029进行拆分，分为管理员帐号管理、配置管理两大类。
重新生成第三章，并审计新生成的第三章的FR设计是否完整且必要。

管理员层和基础层是否有分开的必要？管理员层的系统级配置管理和用户层的配置管理是否有交叉设计的必要？

> 结论是：管理员“层”可以降级为基础层下的管理员治理模块；但管理员系统配置和用户配置必须做交叉设计，重点是 fallback、override、权限、密钥隔离、审计日志和运行时解析规则。

同意结论。因此按照以下指令依序对第三章进行修改：
1. 将管理员层的两个功能下放到基础层的管理员模块
2. 新增管理员系统配置和用户配置的交叉FR设计
3. 将取消的FR进行删除
4. 读取整个FR表，对所有FR进行重新排序和编号

5 系统约束是不是应该放在 03_architecture_decision 里面？

> 所以第 5 章如果现在包含的是技术栈、数据库、框架、外部 API、运行环境、部署限制，应移动到 03_architecture_decisions.md 或 10_operations_deployment.md。02_product_requirements.md 中最多保留一小节“产品约束摘要”，并链接到架构/部署文档，避免需求层混入实现选择。

好，将第5章移动到另外两个文件，只保留链接。

第 6 章 数据需求 是否应该写在数据层设计中？

> 是。第 6 章“数据需求”大部分应该迁移到数据层设计，也就是 [06_data_model.sql](/Users/qingyue/projects/ResearchPaperBase_codex/docs/design/06_data_model.sql) 或配套的数据设计说明文档中。原因很直接：需求文档负责说明“系统需要哪些业务对象和能力”，数据层负责说明“这些对象如何被建模和存储”。如果字段级设计留在 `02`，后续 `06` 和 `07` 很容易出现字段名、枚举、约束冲突。

#TODO

**2026-04-30**

提问：当用户完成了初步构建，切换到对话研究模式，或者主题综述模式，用户期待看到什么？如果在构建模式或综述模式都被使用过之后，在同一主题内用户还可以再次启用这两个Agent吗？如果再次启用的话，用户又期待看到什么？

```markdown
用户完成初步构建后，切到深度研究或主题综述时，期待看到的不是“空白新模式”，而是**基于当前 Project 已构建知识库的可继续工作空间**。

**切到深度研究模式**
用户期待看到：
- 当前 Project 的知识库摘要：有效论文数、最近构建时间、覆盖主题、图谱/向量索引状态。
- 可直接提问的对话入口。
- 历史研究对话列表，如果以前用过该模式。
- 可选输出倾向：创新、实验、总结。
- 引用可追溯：回答中能看到引用论文、图谱节点或相关文段。
- 如果知识库为空或索引过期，应提示回到构建模式补充/更新。

**切到主题综述模式**
用户期待看到：
- 当前 Project 可用于综述的论文库状态。
- 如果第一次使用：进入“生成综述架构/大纲”的起点。
- 如果以前用过：看到已有大纲、章节草稿、审查状态、导出版本。
- 明确的继续入口：继续撰写、重新生成大纲、编辑章节、终审导出。
- 清楚显示哪些内容是用户已确认的，哪些是 Agent 草稿。

可以再次启用构建 Agent 和综述 Agent。关键是“再次启用”不能被设计成覆盖式重启，而应该是**在同一 Project 内增量执行或创建新版本**。

**再次启用构建模式**
用户期待看到：
- 上次构建结果摘要：已入库论文、有效论文、失败项、已推送项。
- 可选择“继续构建/增量更新/重新检索”。
- 复用上次确认过的检索词，也允许修改检索词。
- 新检索结果应与已有论文库去重。
- 新增论文进入同样的评分、解析、分析、入库流程。
- 已存在论文不应重复下载、重复分析或重复推送，除非用户明确选择重新处理。
- 构建完成后，深度研究和综述模式应能使用更新后的知识库。

**再次启用综述模式**
用户期待看到：
- 已有综述项目状态：大纲版本、章节完成度、审查问题、最终导出版本。
- 可选择继续当前综述，而不是只能从头开始。
- 可基于更新后的论文库刷新大纲或章节。
- 刷新不应静默覆盖用户手动编辑内容。
- 应支持创建新综述版本，或者对现有章节进行增量修订。
- 导出时应清楚显示使用的是哪个大纲版本、哪些章节版本、哪些论文引用。

所以设计上应该补一个核心概念：**Project 是长期研究容器，Agent 运行是可重复的任务/会话，不是一次性模式页面。**

更具体地说：

- 构建模式应有 `Build Run` 或 `Construction Task` 概念。
- 深度研究模式应有 `Research Dialogue` 概念。
- 综述模式应有 `Review Draft / Review Version` 概念。
- 模式切换只是切换工作台，不应销毁已有上下文。
- 再次启动 Agent 时，系统应提供“继续、增量、重跑、新版本”这些明确选项。
```

那么对于FR-009和FR-010中包含的论文库侧栏，是否有必要和当前启用的Agent模式相关联？

```markdown
有必要关联，但不应该强绑定。

更准确的设计是：论文库侧栏属于基础层全局组件，但它的**默认内容、可执行动作、状态提示**应根据当前 Project 和当前 Agent 模式变化。

建议规则：

| 当前模式 | 论文库侧栏默认展示 | 可执行动作 |
| --- | --- | --- |
| 构建模式 | 构建结果、候选/有效/失败/未推送论文、当前构建阶段状态 | 补充论文、重试下载/解析、调整有效性、触发构建相关操作 |
| 深度研究模式 | 已入库有效论文、引用频次、图谱/向量索引状态、最近对话引用过的论文 | 查看详情、打开引用来源、跳转图谱；不允许修改论文库或重建图谱 |
| 主题综述模式 | 综述可用论文、章节引用覆盖、未引用重要论文、参考文献状态 | 查看/绑定引用、检查引用覆盖；不允许直接改写构建结果 |

所以它应当是：

- **基础层组件**：任意模式都能打开。
- **Project 绑定**：只展示当前 Project 的论文库。
- **模式感知**：根据当前 Agent 模式显示不同视图和动作。
- **权限/状态受限**：深研和综述模式下原则上只读，构建模式下可编辑/补充/重试。
- **不拥有业务状态**：论文库侧栏不应该自己决定构建、综述、深研状态，只读取 Project、Task、Paper、Citation 等状态。

对 FR 的影响：

- `FR-009 全局界面与状态面板` 应写“论文库侧栏为全局入口，并根据当前模式展示模式相关视图”。
- `FR-010 Project 论文库查看与管理` 应写“论文库管理动作主要在构建模式可用；深研/综述模式默认只读，仅提供引用、详情、跳转和覆盖检查等模式相关动作”。

这样可以避免两个极端：

- 完全无关：用户在综述模式看到一堆构建操作，会造成误操作。
- 强绑定：用户切到深研/综述就看不到论文库，会破坏研究连续性。
```

论文库和Graph关系图谱应当也算作基础层，随时可见并关联到具体论文下载地址，可远程访问打开查阅，而其他的信息如构建模式的论文上传、删除等论文库操作应当放在构建模式中。其他设计按照建议完成。这样更新对于实现而言会不会很复杂？

> 不会显著复杂，反而会降低实现冲突。关键是把“展示/查阅”和“修改/构建”分开。

将这些可视化内容的更新写入到02的FR设计中，完成后显示更改内容。

是否有必要增加新的FR对侧边栏入口进行管理？是否需要在每个Agent模式的FR中对侧边栏显示内容进行增加设计？

这个设计有点乱，重新整理一版逻辑清晰、功能完善的侧边栏显示界面和各界面接口的功能定义。

不，退回对文件的修改，仅仅在设计上进行说明。只针对FR设计，不进行文件修改。

Project 选择、摘要；Agent 模式选择、模式状态应当保留在左边任务窗口侧边栏。论文库入口、Graph图谱入口、导出入口、模式上下文入口应当保留在右边数据窗口侧边栏。我的问题是，固然不同模式只需要将不同的上下文传递到FR-009定义的入口处，但是以何种形式呢？比如构建模式所需的查询结果、论文状态；综述模式所需的引用段落等等，它们传递的信息各不相同，理应采取不同的包装形式才对，而决定传输哪些内容应该也有单独的设计。这种包装或形式的定义应该算作FR设计还是UI设计？

```markdown
这不应该算 FR 设计，应该算 **UI/信息架构设计 + API ViewModel 契约设计**。

FR 只需要定义“用户需要看到什么能力和结果”，例如：

- `FR-009`：系统应提供左右侧栏入口与模式状态展示。
- `FR-010`：系统应提供论文库和 Graph 图谱的只读查阅。
- 构建/深研/综述 FR：系统应在对应模式中展示与当前工作有关的上下文摘要。

但“以什么包装形式传递”“每种模式上下文 payload 长什么样”“字段名是什么”“哪些字段由哪个接口返回”，不应放在 FR。那是设计层的接口契约。

更准确的分层是：

| 问题 | 所属设计层 |
| --- | --- |
| 用户是否需要左右侧栏？ | FR / 产品需求 |
| 左侧栏放 Project、Agent 模式和任务状态？ | UI 信息架构 |
| 右侧栏放论文库、Graph、导出、模式上下文？ | UI 信息架构 |
| 构建模式上下文显示查询结果、论文状态？ | UI 信息架构 + 模式流程设计 |
| 综述模式上下文显示引用段落、引用覆盖？ | UI 信息架构 + 模式流程设计 |
| 前端拿到的数据包装结构是什么？ | API ViewModel 契约 |
| 字段名、枚举、JSON schema？ | API / 数据契约 |
| 不同模式的上下文如何统一抽象？ | UI 状态模型 / API ViewModel |
```

请把我当作完全不了解ViewModel设计的学生，为我讲解这个ViewModel的设计

将 ViewModel 的设计原则写入所需的文件中，但不要将这个初步的ViewModel内容写入。显示修改的文件并解释为什么选择这个文件进行修改。

是否需要在FR中解释向前端暴露什么数据？

> 不需要。FR 不应该解释“向前端暴露什么数据”。

好的，根据当前在不同Agent模式下对前端数据侧边栏显示的需求，修改02的FR设计

将02的第6章 数据需求转移到06_data_*（命名请帮忙生成）的设计文档里。

完成 tasks/002-revirw-all-functional-requirements.md

提问：FR是否需要设定什么数据应当存档或存为数据库？

> 需要，但只需要写到“业务语义层”，不要写成数据库设计。FR 应该说明哪些信息必须被系统保留，因为这影响用户能力、审计、恢复和后续流程。
